本文件夹用于存放在电赛中所需要用到的一些算法
PID，pi控制器，可以用于电压电流环等复用
Park变换，它是反馈通路上的算法。
			它把外部传感器（ADC）采回来的、
			一直在变化的交流信号（$V_\alpha, V_\beta$），
			结合当前的角度 $\theta$，
			“降维”成静态的误差数据（$d, q$），
			然后喂给刚才写的 PI 控制器。
sogi，二阶广义积分器制造波形
dq算法，以park变换为核心的整套旋转坐标系控制算法